#include <cstdio>


int main(){
    int N, d, k, c;
    scanf("%d%d%d%d", &N, &d, &k, &c);

    int a[N];
    for(int i=0;i<N;i++) scanf("%d", &a[i]);

    int l=0, r=l+k;
    while(true){
        for(int i=l;i<r;i++){
            if()
        }
    }
}